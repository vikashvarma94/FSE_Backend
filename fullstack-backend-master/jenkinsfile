pipeline {
    agent any
    stages {
        stage('Commit SCM') {
            agent{
            docker{
            image 'maven'
            args '-v $HOME/.m2:/root/.m2'
            }
            }
            steps {
                echo 'Build'
                bat "mvn clean install"
            }
        }
    }
    }